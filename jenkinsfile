pipeline {
    agent any

    tools {
        maven 'M2_HOME' // Assurez-vous que Maven est correctement configuré dans Jenkins
    }

    environment {
        APP_ENV = "DEV"
    }

    options {
        timeout(time: 1, unit: 'MINUTES')
    }

    stages {
        stage('Checkout code') {
            steps {
                git branch: 'khairidinne',
                    url: 'https://github.com/maramnaderi/devops.git', // Remplacez par l'URL correcte du dépôt
                    credentialsId: '67f03885-84d3-4ed6-b3fb-836d5f2196e8'
            }
        }

        stage('Build') {
            steps {
                sh 'mvn install -Dmaven.test.skip=true' // Commande Maven pour compiler sans tests
            }
        }
    }

    post {
        always {
            echo "======always======"
        }
        success {
            echo "=====pipeline executed successfully====="
        }
        failure {
            echo "======pipeline execution failed======"
        }
    }
}
